import React from 'react'

import LoginButton from '../components/LoginButton'

const Login = (props) => {
    return(
        <div>
            <p>login page</p>
        </div>
    )
}

export default Login