import React from 'react'

import LoginButton from '../components/LoginButton'

const Login = (props) => {
    return(
        <div>
            <LoginButton />
        </div>
    )
}

export default Login