import React from 'react'

import LogoutButton from '../components/LogoutButton'

const Logout = (props) => {
    return(
        <div>
            <LogoutButton />
        </div>
    )
}

export default Logout